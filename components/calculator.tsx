"use client";
import { useState } from "react";
export function AffiliateCalculator(){const [clients,setClients]=useState(12),[value,setValue]=useState(169),[rate,setRate]=useState(30);const total=clients*value*rate/100;return <div className="calculator"><label>Clients actifs <b>{clients}</b><input type="range" min="1" max="100" value={clients} onChange={e=>setClients(+e.target.value)}/></label><label>Abonnement moyen <b>{value} €</b><input type="range" min="39" max="500" step="10" value={value} onChange={e=>setValue(+e.target.value)}/></label><label>Commission estimée <b>{rate} %</b><input type="range" min="10" max="30" step="5" value={rate} onChange={e=>setRate(+e.target.value)}/></label><div className="calc-result"><span>REVENU MENSUEL ESTIMÉ</span><strong>{total.toLocaleString("fr-FR",{maximumFractionDigits:0})} €</strong><small>{clients} clients × {value} € × {rate} %</small></div><p>Estimation non contractuelle. Éligibilité, durée, attribution et paiements dépendent des conditions du programme.</p></div>}
