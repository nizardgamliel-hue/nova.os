import { headers } from "next/headers";import { redirect } from "next/navigation";import { resolveAuthContext } from "@/lib/auth/context";import { ensureWorkspace } from "@/lib/nova/onboarding";import { recommendWorkspace } from "@/lib/nova/recommendations";import { NovaOnboarding } from "@/components/nova-onboarding";import "../platform.css";import "./onboarding.css";import "./mobile-fix.css";
export default async function Page(){const resolved=await resolveAuthContext(await headers(),"companyProfile.read");if(!resolved.context)redirect(resolved.response?.status===403?"/setup":"/login");const data=await ensureWorkspace(resolved.context.organization.id);if(data.organization.onboardingCompleted)redirect("/app");const recommendations=recommendWorkspace({...data.profile,industry:data.organization.industry,currentTools:data.profile.currentTools as Record<string,string[]>});return <NovaOnboarding initial={{...data,recommendations}}/>}
